import { frontWebsiteTheme } from './front-website-theme';

describe('frontWebsiteTheme', () => {
  it('should work', () => {
    expect(frontWebsiteTheme()).toEqual('front-website-theme');
  });
});
